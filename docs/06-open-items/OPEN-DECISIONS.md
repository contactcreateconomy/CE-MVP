# OPEN DECISIONS — Founder Ledger

> **What this is:** the live ledger of decisions that are **OPEN and need a founder/PM call** — deliberately separate from resolved history (`Decisions/_index.md`) and reconciliation logs. Same spirit in which DEC-S20 / DEC-M11-AMAZON were tracked earlier, but scoped to post-register surface-contract work.
> **Not here:** resolved items (they live in `_index.md` §R / `RECONCILIATION-LOG.md`) and screen-count/route questions (inventory is closed — `MASTER-SCREEN-INVENTORY-MERGED.md`).
> **Convention:** when an entry resolves, mark it RESOLVED with date + one-line outcome and move it to the bottom archive section; never delete — the trail matters.

---

## OPEN

| ID | One-line description | CAP-ID chain touched | Wave discovered | Status | Suggested owner |
|---|---|---|---|---|---|
| **E1** | `BetaBanner` is ungoverned — wired into the CAP-025 provider chain but **no CAP owns** its content, copy, visibility rule, dismissibility, or the `systemConfig`/flag key that toggles beta (`signup.mode`/CAP-480 exists but is not a beta switch) | CAP-025 (chain); none owns the banner itself | 1 | OPEN | Founder / PM — decide whether BetaBanner needs a CAP row + config key, or is CAP-less chrome |
| ~~**E2**~~ | ~~Wave-1 CMP slot placeholder undefined — CAP-025 mounts "CMP slot (M18)" but CMP (CAP-504–506) ships Wave 7; what the slot renders before then (nothing vs placeholder) is undefined~~ **→ RESOLVED 2026-08-25 (contract declaration; ledger synced 2026-08-29) — see archive (Doc-sync corrections)** | — | 1 | RESOLVED | — |
| ~~**E3**~~ | ~~Bootstrap-state routing has no owning CAP~~ **→ RESOLVED 2026-08-26 — see archive (Stage-2 audit corrections)** | — | 1 | RESOLVED | — |
| ~~**E4**~~ | Skip-then-stranded has no recovery path — CAP-003 allows skipping the timezone chooser → user stays `pending_context` indefinitely, blocked from protected writes; no CAP defines a re-prompt, reminder, or default-timezone fallback (M14 coach cards CAP-369 are activation nudges, not bootstrap re-prompts) | CAP-003 → CAP-005; no recovery CAP exists | 1 | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #2 | Founder / PM — decide recovery UX (re-prompt, default TZ, or force-complete) |
| ~~**E5**~~ | `/terms` is not grounded in CAP-027 — the row's trigger names only `/privacy` and `/dmca`; the inventory adds `/terms` to the group but no CAP governs it (inheritance assumed, not stated) | CAP-027 (trigger coverage gap) | 1 | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #9 | PM — confirm `/terms` inherits CAP-027 or amend the register with an explicit trigger |
| ~~**E6**~~ | Legal content source & publish trigger undefined — CAP-027 names no content source (DB vs static files), publication flag, version metadata, or what "M18 publish" concretely gates on (no `systemConfig` key exists in any source file) | CAP-027 → M18 publish (no CAP enumerated) | 1 | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #9 | PM / Legal — define storage, publish flag, and version scheme |
| ~~**X1**~~ | ~~**CROSS-CUTTING** — bootstrap-state routing unowned~~ **→ RESOLVED 2026-08-26 with E3 — see archive (Stage-2 audit corrections)** | — | 1 | RESOLVED | — |
| ~~**X2**~~ | ~~**CROSS-CUTTING** — `waitlistEntries` field-level schema unspecified~~ **→ RESOLVED 2026-08-29 (Phase-1 described-vs-applied correction pass / F-13) — see archive** | — | 1 | RESOLVED | — |
| **FUTURE-M2-01** | Remediation sweep for already-published posts using a since-deactivated affiliate link. Not built in Wave 4. Real gap, explicitly deferred, not silently dropped. Recommend before scaling affiliate volume. | CAP-049, CAP-545, `postAffiliateLinks` | 4 | OPEN (tracked future — **not a Wave-4 blocker**) | Founder / PM — schedule before affiliate-volume scale-up |
| ~~**FOUNDER-DECISION-M7-01**~~ | SMS/OTP provider selection required before CAP-551 is buildable. CAP-551 is the `mobileVerified` writer (data-model half closed); delivery mechanism (Twilio or equivalent) is an infrastructure/vendor decision, not a register gap. Same treatment as DEC-M11-AMAZON. Do not invent OTP details. | CAP-551, CAP-141 | 5B | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #1 | Founder — pick provider before CAP-551 is implemented |
| **FUTURE-M7-01** | Username/handle change after initial reservation. Not built in Wave 5 (founder decision: usernames immutable at MVP-1 launch, revisit post-launch if requested). Real, tracked, not silently dropped. | CAP-550, CAP-526, CAP-474 | 5B | OPEN (tracked future — **not a Wave-5 blocker**) | Founder / PM — revisit post-launch if requested |
| **FUTURE-M11-01** | Storefront follow/watch system for revocation notifications. Not built at MVP-1. Real gap, explicitly deferred — revocation still functions and logs without it, only the notification-to-followers step is missing. | CAP-267, CAP-271, `notifications` | 6C | OPEN (tracked future — **not a Wave-6C blocker**) | Founder / PM — revisit when a follow/watch surface is proposed |
| **FUTURE-M12-01** | M12 Has-UI=NO graph / grace / brakes / anti-suppression internals. Metrics zero-state and season/promote (P7E-18) do not require them. Real, tracked, not silently dropped. Do not invent in P7E-03/05. | CAP-282, CAP-286–292, CAP-317 | 7-ECON / SF-03 | OPEN (tracked future — **not a Wave-7 blocker**) | Founder / PM — schedule when graph-damp / loop-brake ops surface is proposed |
| **FUTURE-M12-02** | Collaborative Distribution Networks (`ownershipMode=collaborative`). Register row is **DEFERRED**; co-owner attribution OPEN. Not built at MVP-1. | CAP-320, `distributions.ownershipMode` | 7-ECON / SF-03 | OPEN (tracked future — **not a Wave-7 blocker**) | Founder / PM — after DEC-M12-DN-ATTRIB residual if collaborative DN is ever in-scope |
| **FUTURE-M12-03** | **Dynamic Ladder Tier Management** — founder wants tier names (and possibly count/order) admin-editable rather than a fixed code-level enum. Current implementation is intentionally NOT dynamic: `signal.level` is a compile-time literal enum (`_data-model.md` l.421 Core-enums) mirrored by the exported `SIGNAL_LEVELS` const in `tiered-ladder.tsx`, and the component/test suite guarantee type-safety against that fixed enum. The 2026-09-01 Nebula→Pulsar rename (founder decision) was a spec+code literal edit for exactly this reason. Building this later means: a schema/table for tier definitions, a new admin CRUD screen, and migrating `tiered-ladder.tsx` (and every enum-typed consumer) from the hardcoded enum to a data-driven source — non-trivial scope, not a small follow-up. Not scheduled; do not build any part of it ad hoc. | `signal.level` enum, `signalLevelDefinitions` (already per-season data — names/count stay code-level), `tiered-ladder.tsx` `SIGNAL_LEVELS` | — (post-MVP) | OPEN (tracked future — **not a launch blocker**) | Founder / PM — schedule only with explicit scoping; requires register/data-model/component co-edit |
| **FUTURE-M7-02** | Editor/Publisher/support retrospective review queue (report/hide/restriction/suspension + device-session/related-account/duplicate-campaign). Register Has-UI=YES with "UI: admin queue" but **no inventory row**. Do not invent a third admin board beside `/admin/moderation` (P7E-14) and `/admin/support` (P7A-07/08). | CAP-156 | 7 / SF-03 | OPEN (tracked future — **not a Wave-7 blocker**) | Founder / PM — decide whether this needs its own inventory route or folds into an existing console |
| ~~**F-33**~~ | ~~Legal-intake identity verification + anonymous abuse controls were unspecified (rate/dedupe keys, business-day calendar, retention)~~ **→ RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #6:** statutory identity fields for DMCA/counter-notice (17 U.S.C. §512: legal name, physical address, email, signature attestation), verified-email-only for other forms, 5 submissions/24h per email+IP, (submitter, target, claim-type) 24h dedupe key, US-business-day SLA clock. | CAP-217, CAP-343, CAP-344, CAP-348, CAP-350 | 7-TRUST / P7T-05/06 | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #6 | — |
| **F-34** | **Eligibility-state routing unowned** — distinct from the resolved E3/X1 bootstrap-routing gap (Platform-Wide Routing Convention, CONTRACT-1-app-shell §1, explicitly scoped OUT of M7 profile-completion routing). No CAP or contract owns: (a) Composer → `/setup` navigation when CAP-140 returns `basic_incomplete` (server behavior is fully specified — draft preserved, missing decisions returned — only the client redirect-on-response is unowned); (b) `/setup` → away navigation for an already-`basic-profile-complete` member. **NON-BLOCKING-BUT-REAL** (server enforcement is fail-safe either way; a missing redirect degrades UX, not security). Do not fold into the bootstrap-routing convention — eligibility state and bootstrap state are different gates. | CAP-140 (eligibility.check, post path); CAP-142 (profile.upsertBasic); `/setup` (no destination-owning CAP found) | 2 / 5B (raised in Stage-2 audit follow-up session, 2026-08-26) | OPEN | Product/spec — write a second, separately-named routing convention (or two contract sentences) for `postingEligibilityState` navigation; affects `CONTRACT-2-compose-FINAL.md` OQ7 and `CONTRACT-5-setup-FINAL.md` OQ5, both already worded accurately as open — no contract edit required until this is resolved |
| ~~**F-35**~~ | **`moderationCases.policyFamily` enum undefined** — not aliasable to `strike.class` without founder confirmation; they serve different purposes (`strike.class` = consequence taxonomy; `policyFamily` = INV-2 dedupe grouping key). Related but likely not identical value sets. Needs a small dedicated enum, informed by but not copied from `strike.class`. M13 sheet §2 l.20–30 has no `policyFamily:` line (F-19 remainder: `caseType`/`status` transcribed to the bible 2026-08-29; this field was the sheet gap). **NON-BLOCKING** for Phase 7 sub-batches already sliced (P7E-10/13/14 reference the field structurally without needing its literal values yet); **BLOCKING** for anyone implementing the actual INV-2 dedupe query logic. | `moderationCases.policyFamily` (INV-2); `strikes.class`; CAP-324 (`report.submit` many→one per target+policyFamily+window); CAP-321–328 writers | 7-ECON (raised 2026-08-29, F-19 remainder / M13 §2 gap) | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #4 | Founder — write a dedicated `policyFamily` enum (informed by, not copied from, `strike.class`); then back-fill `_data-model.md` Core-enums + the `moderationCases` bullet |
| **F-36** | **Waitlist capture has two member-facing surfaces for the same underlying write** — CAP-001 waitlist-mode on `/signin` (email capture only, no user/role; Writes `waitlistEntries`) vs CAP-014's dedicated `/waitlist` page (`waitlist.join` publicMutation; same write, same "not a users row; no role" invariant). Flagged in the original Wave-1 process and restated as `CONTRACT-1-signin-FINAL.md` OQ#3 / P2-04 fence ("does waitlist-mode `/signin` call `waitlist.join` or a distinct mutation? Register gives the write target, not the call"). **NON-BLOCKING** — both paths currently work independently; this is a maintainability/consistency question, not a correctness gap (neither path invents a users/role write; neither emits L08 `signup_completed`). Distinct from **F-14** (landing waitlist CTA; **P7T-12** is the slice that delegates to CAP-014 when built — F-14 is not a row on this ledger). Do not silently merge the screens in a slice. | CAP-001 (`/signin` waitlist-mode branch); CAP-014 (`/waitlist` · `waitlist.join`); CAP-015 (shared rate keys if they share the mutation) | 1 / 2 (signin OQ#3; P2-04 fence; ledgered 2026-08-29 during 7-TRUST) | OPEN | Founder — decide whether to consolidate to one canonical capture surface (and whether `/signin` waitlist-mode must call CAP-014 rather than a second mutation) |
| ~~**F-37**~~ | **`users.activationProgress` 7 bits unresolved** — M14 R-LADDER / CAP-368 require "7 bits tracked server-side"; the confirmed M14 sheet leaves `{…}` and does not name them. Rule prose names three related writes (`resourceAcquired`, `day7Returned`, `commentCreated`) but not a 7-bit shape. The only candidate names (interest≥1 · rules · first_acquire · first_comment · first_valuable · first_save · first_return_d7) are an unverified collection-prompt / Opus panel suggestion, never locked. Bible correctly withholds invented values (Core-enums records the gap). **NON-BLOCKING structurally**; **BLOCKING** for whoever implements the actual ladder progress computation (CAP-368). | `users.activationProgress`; CAP-362 (init bits); CAP-368 (R-LADDER); M14 R-LADDER | M14 / Phase 1 (stop-and-report at P1-01b; ledgered 2026-08-29) | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #3 | Founder / PM — lock the 7 bit names; then back-fill `_data-model.md` M14/`users` bullets + Core-enums |
| **F-38** | **Support-scoped intervention inbox has no owning capability** — CAP-408 Notes say "Support sees only support-scoped alerts"; home OQ#2 and support OQ#4 echo the same surface. No CAP implements that filtered view. What exists is the full Administrator intervention list on `/admin/home` (CAP-407 create · CAP-408 ack · CAP-409 resolve · CAP-410 snooze). Distinct from CAP-408 itself (Administrator `intervention.ack` — correctly homed on Home / P7A-03; not orphaned). **NON-BLOCKING** (a UX/scoping nicety, not a hard gap — support operators can see Home if granted access). Currently homeless. Do not invent a support inbox in a slice. | CAP-408 Notes (referenced surface, not the ack mutation); CAP-407–410 (`/admin/home`); `CONTRACT-7-admin-home-FINAL.md` OQ#2; `CONTRACT-7-admin-support-FINAL.md` OQ#4 | 7-AdminCore (raised 2026-08-29; home/support OQ leftovers from Wave 7B) | OPEN | Founder / PM — decide whether this needs its own capability or whether Home's existing list should filter by role |
| ~~**F-22**~~ | **`jobRuns.manual_review` has no human action surface; redrive-vs-RC-4 semantics unstated** — AUDIT-FINAL F-22 (VERIFIED, BAS; reliability OQ#1/OQ#2). CAP-495/515 write `cancelled`/`manual_review` for authz-fail and RC-4 (`legal restore · money · sanction · final publish · permanent termination`) with Has-UI=YES but no inventory placement. CAP-500 specifies ordinary dead-letter redrive; it does **not** say whether redriving a `manual_only` failure is permitted or whether it resets attempts. **Was missing from this ledger until 2026-08-29** (audit-only; not resolved). **NON-BLOCKING for list/status display**; **BLOCKING for anyone who would redrive or "approve" those rows.** 7-OPS P7O-04 shows `manual_review` read-only; P7O-05 fail-closed **rejects** redrive of `retryClass=manual_only` and `state=manual_review`. That fence is not a resolution. | CAP-495, CAP-515, CAP-500; `jobRuns.state=manual_review`; `retryClass=manual_only`; `CONTRACT-7-admin-reliability-FINAL.md` OQ#1/#2; AUDIT-FINAL F-22 | 7D / Stage-2 audit (ledgered 2026-08-29 during 7-OPS) | RESOLVED 2026-09-04 — DECISIONS-LOCKED.md #5 | Founder / PM — decide (a) where a human actions `manual_review` rows and (b) whether CAP-500 may redrive RC-4 / `manual_only` failures |
| **FUTURE-M5-01** | **Editor-defined scenario presets on Compare** — fixed/canonical scenario callouts authored by the editor (e.g. "Best for solo creators," "Best for teams"), rendered on compare bodies; **never reader-adjustable**. Reader-side weight sliders / reactive grids were archived (product-fit review 2026-08-31, `archive/cluster1-interactive-mechanics/`). Canonical presets echo the spec's editor-authored-content philosophy. | Compare extension data (`scenarios` on compare bodies); CONTRACT-2-post-detail (compare body) | Product-fit review 2026-08-31 (CEMVP repo triage, cluster 1) | OPEN (tracked future — **not a launch item**) | Founder / PM — schedule when Compare bodies ship with editor tooling |
| **FUTURE-M5-02** | **Facet-sort on compare tables** — let readers sort a compare table's rows by a single criterion facet (column sort), without any score recomputation or weighting. The reactive "Show differences only" diff-hiding was archived with cluster 1; facet-sort is the narrow, non-interactive-mechanic successor. | Compare tables (same surface as FUTURE-M5-01) | Product-fit review 2026-08-31 (cluster 1) | OPEN (tracked future — **not a launch item**) | Founder / PM — bundle with FUTURE-M5-01 |
| **FUTURE-M6-01** | **MAX-assisted debate structuring** — MAX proposes a position frame for a debate (motion + for/against skeleton); the user confirms/edits before publishing. **Not a composer requirement** — typed debate-entry compose forms were archived (2026-08-31, `archive/cluster2-compose-forms/DebateComposeForm.tsx`). Reuses the archived form's field structure as the proposal shape. | Debate post type; compose flow; M8 MAX | Product-fit review 2026-08-31 (cluster 2) | OPEN (tracked future — **not a launch item**) | Founder / PM — schedule when MAX-assisted composition is in scope |
| **FUTURE-M6-02** | **Reply-as-review** — members reply to a tool discussion with a structured review entry instead of a plain comment. **Explicit trigger: build when median reviews-per-tool OR MAX-divergence-view-share crosses a threshold to be set from real post-launch data — not a launch item.** When built, it **reuses the existing review pipeline (verification, sources, moderation)** — do not re-derive from scratch; the archived form (`archive/cluster2-compose-forms/ReviewComposeForm.tsx`) is the field-structure reference. | Review pipeline (CAP-110/535 class); discussion-thread replies; M3 qualification | Product-fit review 2026-08-31 (cluster 2) | OPEN (tracked future — **data-triggered, not scheduled**) | Founder / PM — revisit when the trigger metric crosses threshold post-launch |

---

## Status pass — 2026-08-29 (7-OPS / slice-decomposition close)

Every remaining OPEN row above was re-read. None silently closed. **F-22** added this pass (had lived only in AUDIT-FINAL). **F-13** stays archived as X2. Audit **F-14 / F-16 / F-17 / F-33** remain aliased (P7T-12 slice / E6 / E1 / 7-TRUST fences) — not duplicate F-rows.

**Slice-decomposition leftover close (same day, not new F-rows):** CAP-334 landed on existing **P7A-06** (register **M13** — not M9/M18). CAP-453/454 landed as new **P7O-08**. Filling `consentRecords` does **not** unblock 453/454 (different table; still P7T-13). CMP OQ#3 (CAP-506 wire vs 453/454) remains a P7T-13 fence, not **F-39**.

**SF-03 remainder (same day, FUTURE rows not F-rows):** **FUTURE-M12-01** (CAP-282/286–292/317 Has-UI=NO internals), **FUTURE-M12-02** (CAP-320 register-DEFERRED collaborative DN), **FUTURE-M7-02** (CAP-156 retrospective queue — no inventory row; do not invent a third admin board). CAP-012/013/361/393/400/423/429/431/433/444/450 homed on existing or new slices this firing — not ledgered as F-rows.

## Status pass — 2026-08-31 (CEMVP product-fit review close)

Four **FUTURE** rows added from the founder's closed product-fit review of the pre-spec CEMVP repo
(`REPO-TRIAGE.md` §"PRODUCT-FIT REVIEW — CLOSED"): **FUTURE-M5-01** (editor-defined scenario
presets on Compare — fixed/canonical, never reader-adjustable), **FUTURE-M5-02** (facet-sort on
compare tables), **FUTURE-M6-01** (MAX-assisted debate structuring — proposal-then-confirm, not a
composer requirement), **FUTURE-M6-02** (reply-as-review — explicit data trigger; reuses the
existing review pipeline). All four are tracked-future, not launch items. The corresponding built
code was archived with provenance (`CEMVP/CEMVP/archive/`), never deleted. No prior OPEN row was
affected.

---

## RESOLVED ARCHIVE

### Wave 2 escalations (resolved by founder decision, 2026-08-23)

*(Numbered W2-E1…W2-E6 to avoid colliding with the still-open Wave-1 E1–E6 above, which are different issues.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W2-E1** | News excluded from member composer — platform-injected via M2 ingestion, not member-authored; "submit news tip," if wanted later, is a distinct unspecified capability | `postNews` removed from CAP-086 Writes + Notes; `CONTRACT-2-compose-FINAL.md` (7 member-composable types) |
| **W2-E2** | Member post-tagging added — `postTags` join set/edit in compose (CAP-530), read for display on post detail (CAP-090 Reads + Notes) | **CAP-530 (new)**; CAP-090 amended; `CONTRACT-2-compose-FINAL.md`, `CONTRACT-2-post-detail-FINAL.md` |
| **W2-E3** | Draft capability added — draft save (CAP-531) + My Drafts list (CAP-532); draft-save now a specified composer action. **Sub-question remains OPEN:** My Drafts = own screen-inventory row vs. filtered view in an existing screen — founder/PM call | **CAP-531, CAP-532 (new)**; `CONTRACT-2-compose-FINAL.md` |
| **W2-E4** | `verdictScore` is computed or editorially assigned, never a direct member write — members submit review content only (self-serving score-inflation risk; protects M3 qualification signal integrity). *Closed out 2026-08-23 (Item #4):* `verdictScore` = auto-computed same-transaction average of member per-dimension scores (`value_for_money` excluded when `not_applicable`); tool-level curated verdicts = **CAP-535** (new), homed on `tools` editorialVerdict* fields | CAP-086 Writes + Notes; **CAP-535 (new)**; CAP-110 amended; `CONTRACT-2-compose-FINAL.md`, `CONTRACT-2-tool-profile-FINAL.md`; `_data-model.md` (`tools`, `postReviews`) |
| **W2-E5** | Rating auto-flag mechanism added — System flags on threshold breach (velocity/outlier pattern), `moderationCases` targeting the `toolRatings` row; admin-configurable threshold, exact formula deliberately unspecified | **CAP-533 (new)**; `CONTRACT-2-tool-profile-FINAL.md` |
| **W2-E6** | `not_applicable` scoped to `value_for_money` only (enum note authoritative) — ease-of-use/output-quality/reliability always ratable | `_data-model.md` field prose corrected; `CONTRACT-2-tool-profile-FINAL.md` rating form |

*Standing rule adopted alongside (root gap c):* **Public Read Queries** — every query serving both anonymous and member actors defines two explicit branches (anonymous-safe view + full member view); inherited by default by Actor = "anonymous, member" capabilities from Wave 2 onward. Recorded in `_data-model.md` §Locked structural principles.

*(Wave-1 entries: E2, E3 + X1, X2 resolved — see "Stage-2 audit corrections" / "Doc-sync corrections" / "Phase-1 bible correction" at the bottom; E1/E4/E5/E6 remain OPEN above.)*

### Wave 3 escalations (resolved by founder decision, 2026-08-23)

*(Numbered W3-E1…W3-E6. All six resolved — W3-E4 closed in a founder second pass the same day.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W3-E1** | `configKeyRegistry` is the authoritative threshold-bounds owner (CAP-395 pattern — one validation mechanism platform-wide); `qualificationRules.thresholdConfig` deprecated as a bounds-owner, stores live values only | CAP-068/070/071/072/074 Notes + CAP-084; `CONTRACT-3-rulebook-FINAL.md` §1/§2/§4; `_data-model.md` (`qualificationRules`, `configKeyRegistry`) |
| **W3-E2** | H-TYPE mapping completed to 8/8 active types (Help→problemStatement; Spark→statement ≤280 chars per CAP-186/§4); H-TYPE declared a structural contract — per-type required-field list editor, not a numeric slider | CAP-074 Notes; `CONTRACT-3-rulebook-FINAL.md` §3-S7/§4/§6 |
| **W3-E3** | **CAP-536 added** — System seeds `qualificationRules` with baked default threshold values at deploy (all 7 tunable rows, once, not user-triggered); `/admin/rulebook` empty state unreachable by design | **CAP-536 (new)**; register 535→536; `CONTRACT-3-rulebook-FINAL.md` Open Questions #6 |
| **W3-E5** | Minimal admin auth gate (basic role check) at Wave 3; full M15 `/admin` shell (CAP-390/392) wraps at Wave 7 — anticipated in Phase 2's screen-inventory sequencing, not a gap | CAP-084/085 Notes; `CONTRACT-3-rulebook-FINAL.md` §1 |
| **W3-E6** | Register hygiene reconciled — Admin-Config count 23→25 (CAP-533/534 counted); CAP-068/070/071/072/074 flipped CANDIDATE→CONFIRMED; inventory §4 refreshed for CAP-533/534; register now self-amending via inline stamps, sync rule recorded | `CAPABILITY-REGISTER-MERGED.md` (stats + 7 rows); `MASTER-SCREEN-INVENTORY-MERGED.md` §4; `RECONCILIATION-LOG.md` §12 |
| **W3-E4** | Calibration design closed (founder second pass, 2026-08-23): **`calibrationExamples`** entity added (candidateSnapshot, expectedOutcome pass/fail per rule, addedBy, addedAt) — the labeled set CAP-085 replays; **`source {live\|replay}` discriminator** added to `qualificationRuleResults` (live = immutable per CAP-083; replay = CAP-085's domain — no conflict); CAP-083/085 Notes amended (per-source immutability; Actor=System + admin trigger declared intentional: admin initiates, system executes); **CAP-537 added** (administrator curates calibrationExamples — distinct from triggering calibrate); calibration-set management is in `/admin/rulebook` scope | **CAP-537 (new)**; CAP-083/085 amended; `_data-model.md` (`calibrationExamples` + discriminator); `CONTRACT-3-rulebook-FINAL.md` §2/§3-S2/§4/§7 |

### Wave 4 escalations (all six closed — founder 2026-08-24)

*(Numbered W4-E1…W4-E6. E2/E4 closed first pass; E1/E3/E5/E6 closed second pass the same day.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W4-E1** | **CAP-538 added** (M2, Publisher, Has-UI=YES) — lists `sources` for `/admin/sources`. CAP-031 remains the register/edit write (Reads=none). Table load is now a specified action in `CONTRACT-4-sources-FINAL.md`. | **CAP-538 (new)**; `CONTRACT-4-sources-FINAL.md` §2/§4; inventory `/admin/sources` Primary CAP-IDs |
| **W4-E2** | Minimal block/unblock toggle ships now on `/admin/sources` (`sources.trustLevel`, simple CAP-031 admin action); full escalation-to-legal-takedown (CAP-059) wires at Wave 7 when `/legal/intake` exists — Wave-3 E5 minimal-now/full-later pattern | CAP-031 Notes; CAP-059 Notes; `CONTRACT-4-sources-FINAL.md` §1/§4 |
| **W4-E3** | **Scope expansion applied mid-wave:** `/admin/affiliate-inventory` added (inventory, Wave 4, M2, Admin config panel). **CAP-539/540/541 added** (create/edit `commercialEntities` → `affiliateRelationships` → `affiliateLinks`). **CAP-544 added** (list read — Flag 1a). **auditLog added to CAP-539/540/541 Writes** (Flag 1b). CAP-049 Notes: inventory populated via those CAPs. **Screen contract deferred.** Flags 1a/1b were Scribe-audit residuals, not panel misses. | **CAP-539, CAP-540, CAP-541, CAP-544 (new)**; CAP-539/540/541 Writes+Notes; CAP-049 Notes; `MASTER-SCREEN-INVENTORY-MERGED.md`; `RECONCILIATION-4.md` |
| **W4-E4** | CAP-173/174/175 (persona approve/reject/schedule, M8) are **explicitly out of scope** for `/admin/editorial` — they belong to Wave 5. CAP-048 regen stays on this screen. Intentional deferral, not a gap (same pattern as `/p/[slug]` Wave-5/6 deferred sections) | `CONTRACT-4-editorial-FINAL.md` §3-D / §7 item 1 |
| **W4-E5** | **CAP-542 added** — Editor confirms/rejects entailment on individual `draftClaimRefs` (`operatorConfirmed`). CAP-043 Gated by CAP-542; approve must not fire while any ref is false/unset (server-enforced). **CAP-543 added** (Flag 2) — Editor manually edits candidate draft (distinct from CAP-042 regen); triggers CAP-045. CAP-045 Gated by retargeted to CAP-543. Flag 2 was a Scribe-audit residual, not a panel miss. | **CAP-542, CAP-543 (new)**; CAP-043 Gated by + Notes; CAP-045 Gated by + Notes; `CONTRACT-4-editorial-FINAL.md` |
| **W4-E6** | `contentCandidates.rejectionReason` (string, required on reject) added. CAP-044 Writes include it. Candidate field = retained legal-audit record (non-deletable); `auditLog.reasonCode`/`justification` = separate generic trail, not a duplicate. | `_data-model.md` (`contentCandidates`); CAP-044 Writes + Notes; `CONTRACT-4-editorial-FINAL.md` reject action |

### Wave 4B escalations (`/admin/affiliate-inventory` — all six closed, founder 2026-08-24)

*(Numbered W4B-E1…W4B-E6. Distinct from Wave-4 E1–E6. FUTURE-M2-01 stays OPEN by design — tracked future item, not a Wave-4 blocker.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W4B-E1** | Prevention now: CAP-049 injection-time check (`relationshipStatus=active` AND link `status=active`). **CAP-545 added** (soft-deactivate + downward cascade). Already-published posts are **not** retroactively stripped — that remediation is **FUTURE-M2-01** (named, not built). | **CAP-545 (new)**; CAP-049 Notes; `_data-model.md` (`affiliateLinks.status`); `CONTRACT-4-affiliate-inventory-FINAL.md` |
| **W4B-E2** | `affiliateRelationships.relationshipStatus` enum `{active, paused, terminated}` added. CAP-540/CAP-049 Notes cite it. CAP-049 injects only when `=active`. | `_data-model.md`; CAP-540 Notes; CAP-049 Notes |
| **W4B-E3** | `affiliateLinks.url` validation cites Showcase **CAP-100** + M11 **CAP-235** (not reinvented). | CAP-541 Notes; `CONTRACT-4-affiliate-inventory-FINAL.md` |
| **W4B-E4** | Inventory Actor(s) for `/admin/affiliate-inventory` corrected to **administrator** only (register CAP-539/540/541/544/545). Publisher removed. | `MASTER-SCREEN-INVENTORY-MERGED.md` |
| **W4B-E5** | `commercialEntities.logoAssetId` reuses existing M1 **CAP-012** `generateUploadUrl` (flag+MIME+size → `mediaAssets`). No duplicate upload CAP. | CAP-539 Notes; CAP-012 (existing) |
| **W4B-E6** | Remaining types published: `commercialEntities.status` {active, inactive}; `affiliateLinks.disclosureClass` {sponsored, affiliate, paid}; `commissionModel` {cpa, cps, cpc, revshare, flat, other}; `cookieWindow` integer days. Last four are reasonable defaults, not register citations. | `_data-model.md` Commercial section |

### Wave 5A escalations (`/personas` batch — all eight closed, founder 2026-08-24)

*(Numbered W5A-E-A…W5A-E-H. Distinct from Wave-1 E1–E6.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W5A-E-A** | CAP-181 Reads `(UI routes to CAP-157)` → **`(UI routes to CAP-176)`**, matching Gated by. | CAP-181; `CONTRACT-5-personas-FINAL.md` |
| **W5A-E-B** | `personaCommentDrafts.status` gained `scheduled`; `scheduledFor?` added (CAP-175 write; same status-enum pattern as `contentCandidates` / CAP-054). | `_data-model.md`; `CONTRACT-5-personas-queue-FINAL.md` |
| **W5A-E-C** | CAP-173 Gated by self-reference → **CAP-175**. | CAP-173; queue contract |
| **W5A-E-D** | CAP-164 Writes "optional final comment via CAP-154" → **via CAP-173** (`personaComment.approve/publish`). | CAP-164; `CONTRACT-5-admin-personas-FINAL.md` |
| **W5A-E-E** | Explicit sentence: inventory Actor(s) = broadest screen access, not uniform per-action authority. Per-action gating already correct. Footnote on Wave 5 inventory. | `CONTRACT-5-admin-personas-FINAL.md`; `MASTER-SCREEN-INVENTORY-MERGED.md` |
| **W5A-E-F** | **CAP-546** admin genome history + rollback. **CAP-547** System invalidates CAP-158 `(in-memory compiled prompt)` + `personas.systemPrompt` on edit or rollback. | **CAP-546, CAP-547 (new)**; genome contract |
| **W5A-E-G** | **CAP-548** admin preview from draft/unsaved genome; writes existing `personaGenomeEdits.previewFixtureRef`. | **CAP-548 (new)**; genome contract |
| **W5A-E-H** | Genome public-safe allowlist in `_data-model.md` (CAP-394-class firewall). CAP-180 Notes bound public projection to it. | CAP-180 Notes; `_data-model.md`; profile contract |

### Wave 5B escalations (`/setup` `/settings/profile` `/u/[handle]` — E-settings-1/2/3, E-setup-2, E-handle-1, W5B-AUDIT-1 closed; E-mobile data-model half closed, founder 2026-08-25)

*(Numbered W5B-E-* to avoid colliding with Wave-1 E1–E6 and W5A-E-A…H.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W5B-E-settings-1** | CAP-149 Writes "triggers CAP-132 erasure cascade" → **triggers CAP-151**. | CAP-149; `CONTRACT-5-settings-profile-FINAL.md` |
| **W5B-E-settings-2** | **CAP-549** `social.revoke` — member soft-deletes a linked social handle (`revokedAt`, `deletedAt`). | **CAP-549 (new)**; settings contract |
| **W5B-E-settings-3** | CAP-157 accepting mutation named **`consent.reaccept`**. | CAP-157; settings contract |
| **W5B-E-setup-2** | CAP-142 Writes gained `users (rulesAcceptedVersion, legalAgeAssertedVersion)`. | CAP-142; setup contract |
| **W5B-E-handle-1** | **CAP-550** System `handle.reserve` writes `users (username, usernameNormalized)`. Handle CHANGE deferred as **FUTURE-M7-01**. | **CAP-550 (new)**; u-handle contract |
| **W5B-E-mobile** | **CAP-551** `mobile.verify` writes `users (mobileVerified=true), privateUserData (mobileNumber)`. OTP provider stays OPEN as FOUNDER-DECISION-M7-01. | **CAP-551 (new)**; setup + discussion contracts |
| **W5B-AUDIT-1** | **CAP-552** `profile.togglePrivacy` writes `users (profileVisibility, leaderboardOptOut)`. Combined: same screen, same actor, same audit sensitivity. | **CAP-552 (new)**; settings contract |

### Wave 6A escalations (`/feed` `/admin/curation` `/search` — E1–E6 closed; CAP-553/554 standing-rule residuals, founder 2026-08-25)

*(Numbered W6A-E* to avoid colliding with Wave-1 E1–E6.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W6A-E1** | CAP-198 Gated by **182/183/184/185** (not 164/165). | CAP-198; feed contract |
| **W6A-E2** | CAP-201 routes to **CAP-194** (Podium, not CAP-175). | CAP-201; feed contract |
| **W6A-E3** | CAP-195 Gated-by CAP-173 **removed** (unrelated persona-comment approval). | CAP-195; feed contract |
| **W6A-E4** | `cardSummaries` gained runningComment/avatars fields; pre-compute wins over read-at-render. | `_data-model.md`; CAP-196/197; feed contract |
| **W6A-E5** | CAP-185 Reads gained **`commentSaves`**. | CAP-185; feed contract |
| **W6A-E6** | CAP-529 Reads: handle/displayName on **`users`**, not `profiles`. | CAP-529; search contract |
| **W6A-CAP-553** | **CAP-553** `feed.unhide` — member reverses hide/mute on `feedSessions`. | **CAP-553 (new)**; feed contract |
| **W6A-CAP-554** | **CAP-554** `vibing.pullFeatured` — admin sets `vibingFeatured.status=pulled`. | **CAP-554 (new)**; curation contract |

### Wave 6B escalations (`/resources` `/resources/[slug]/view` `/contribute` `/admin/resources` — E1–E8 closed; CAP-555–559 status transitions + `pilotKillGateEvaluations`, founder 2026-08-25)

*(Numbered W6B-E* to avoid colliding with Wave-1 E1–E6.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W6B-E1** | Every M10 write formerly targeting `dmcaNotices` now writes **`legalIntake`**. Confirmed no remaining M10 create of `dmcaNotices`. | CAP-217/218 Writes; CAP-220/225 Reads; `_data-model.md`; admin-resources contract |
| **W6B-E2** | CAP-212 quota basis = **user-local calendar, per DEC-S19 (CONFIRMED)**. UTC hedge removed. | CAP-212; resources contract |
| **W6B-E3** | `/contribute` is reachable and renders a **disabled state** (not 404) when `ugc.enabled=false` — Wave 1 `unavailable_pending_legal` pattern. Flag wired into CAP-202 submit gate. | CAP-202/203; contribute contract |
| **W6B-E4** | CAP-207 Writes include **`auditLog`**. Operator-triggered System action, same audit pattern as CAP-042/CAP-053. | CAP-207; admin-resources contract |
| **W6B-E6** | Inventory `/admin/resources` Actor(s) includes **`support_operator`**. Actor(s) = broadest access, not uniform authority. CAP-221 Administrator-only kill-switch is a distinct narrower gate. | inventory; CAP-221; admin-resources contract |
| **W6B-E7** | **CAP-555–559** own the previously unwritten transitions: pause / archive / under_legal_review / review / version editorial_review→approved. Enums kept. | **CAP-555–559 (new)**; admin-resources contract |
| **W6B-E8** | New entity **`pilotKillGateEvaluations`** (none existed). CAP-220 writes it. Thresholds admin-configurable (toggle > hardcode). CAP-221 remains the disable write. | `_data-model.md`; CAP-220; admin-resources contract |

### Wave 6C escalations (M11 Affiliate Storefront — 6 screens, money path; E1/E2/E4/E6 closed + E3→FUTURE-M11-01 + E5/E7 investigation-applied, founder 2026-08-25)

*(Numbered W6C-E* to avoid colliding with Wave-1 E1–E6 and W6B-E*.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W6C-E1** | CAP-247 BUY gate retargeted **`storefrontLinks.status=active` (nonexistent field) → `storefrontLinks.validationState=approved_locked`**. Register-wide grep post-fix: no other `storefrontLinks.status` occurrence (CAP-049's `affiliateLinks.status=active` is a different entity with a real `status` field — correct, untouched). | CAP-247; go-redirect contract (Route & Access + Actions); admin-store contract propagation note |
| **W6C-E2** | CAP-525 Writes corrected to **`type=self_report` AND `status=unverified` (two fields)** — no `self-reported-unverified` literal exists in the `salesEvidence` enums. | CAP-525; sell contract (States F + Actions) |
| **W6C-E3** | Followers/watchers notification **deferred as FUTURE-M11-01** (see tracked-future table above). CAP-267/271 Notes qualified — revocation + auditLog unaffected; public-notice + consented-buyer branches are MVP-1 behavior. | CAP-267/271; admin-store contract; OPEN-DECISIONS FUTURE table |
| **W6C-E4** | CAP-233 gated-by corrected **CAP-248 → CAP-237** (stale cross-ref; activation needs ≥1 approved product, which CAP-237 produces). | CAP-233; sell contract |
| **W6C-E5** | **Investigation finding:** `storefronts.status` **already has `paused`** (owner-initiated, CAP-270 `store.pause`, member-actor, immediate/no-review) distinct from operator `suspended`/`closed` (CAP-264/265). **No new enum value, no new CAP** — the owner-hide capability was already wired; finding documented in the storefront contract. | storefront contract (Route & Access + States B + Action row); no register change needed |
| **W6C-E6** | CAP-230 eligibility formula now includes **"not staff, not persona"** — R-STAFF pattern (CAP-112, Wave 2), same conflict-of-interest logic, server-side reject. | CAP-230; sell-apply contract |
| **W6C-E7** | **Investigation finding:** CAP-258/259 are **genuine mislabels, not intentional splits** — trigger, actor, and writes are all member-side (seller-submitted evidence rows; no System-side computation), structurally identical to CAP-260 (member) on the same surface. Contrast CAP-207/Wave-6B-E4 where System actually derives. **Actors corrected System → member.** | CAP-258/259; sell contract |

### Wave 7A escalations (Trust/Legal/Onboarding/Consent — 7 surfaces; E1–E4 closed, founder 2026-08-25)

*(Numbered W7A-E* to avoid colliding with Wave-1 E1–E6 and prior wave prefixes.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
|| **W7A-E1** | `takedownRequests` ⛔ **ABSORBED** into `legalIntake` — marker at data-model line 169 (matching dmcaNotices' line-199 pattern); CAP-058 Writes → `legalIntake (type=source_takedown)`, CAP-059 Writes → `legalIntake (status=actioned/resolved)`, **CAP-065 Reads → `legalIntake`** (H-SRC check; read-retarget mirrors Wave-6B's CAP-220/225 dmcaNotices precedent). Register-wide grep post-fix: zero takedownRequests readers/writers remain (3 string-hits all inside fix Notes). | CAP-058/059/065; `_data-model.md` line 169; legal-intake contract |
|| **W7A-E2** | M7 consent mutations renamed: CAP-148 `consent.record` → **`profile.consentRecord`**, CAP-149 `consent.withdraw` → **`profile.consentWithdraw`**. M18's CAP-505/506 `consent.record`/`consent.withdraw` stay **canonical platform-wide CMP names, unchanged**. CAP-157 note corrected (prior "matches M7/M18" claim was the collision itself). Ripple propagated: wave-5 setup + settings-profile contracts, M7 bible, MASTER-DECISION-DRAFT. Residual (functional, not naming): CAP-506↔CAP-149 withdrawal interaction remains open. | CAP-148/149/157; CONTRACT-5-setup, CONTRACT-5-settings-profile; M7-posting-profile.md; MASTER-DECISION-DRAFT.md; cmp contract |
|| **W7A-E3** | CAP-339 Reads restricted **`strikes, users` → none** — the public /repeat-infringer page reads policy text + precomputed aggregate statistics ONLY ("N repeat infringers actioned this period", no identities). Firewall note: consistent with CAP-394 / Wave-5A E-H — per-user moderation data never surfaces on an unauthenticated public route. | CAP-339; repeat-infringer contract (Entities/States) |
|| **W7A-E4** | **CAP-562** (`/about`) + **CAP-563** (`/help`) added — M17, anonymous, Reads/Writes none, exact CAP-027 pattern (static render, no API, legal-layout family, no-ConsentProvider). Register 561→563. Content-source/publish-trigger remain Open Questions (Wave-1 E5/E6 class — founder content process). | **CAP-562, CAP-563 (new)**; trust-pages contract |

### Wave 7B escalations (M15 Admin Shell — E1–E6 all closed, founder 2026-08-26)

*(Numbered W7B-E* to avoid colliding with prior wave prefixes. Applied in dependency order: E2 first, then E1, E5, E3, E4, E6.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W7B-E2** | CAP-390/392 Actor broadened Administrator → **any staff role** (canonical `roleAssignments.role` minus `member`: editor · publisher · moderator · store_operator · support_operator · administrator — data-model line 266; Founder = administrator + founder-only keys, not a separate enum). `assertAdminPermission` = "is this user ANY staff role" — **shell entry only**. Two-layer model stated on CAP-390: shell entry (broadest) vs each screen's own per-CAP Actor column (narrow, unchanged). **Leak verification performed:** only the shell/route rows broadened — CAP-394/395/396 (Administrator), CAP-397/398 (Founder/Admin), CAP-413 (Founder), CAP-408–410/421/422 (Administrator), CAP-221, CAP-557 all retain their narrow Actors. No staff access granted to Administrator/Founder-only surfaces. | CAP-390/392; shell contract (header, §1, States A/B, OQ#1/#2) |
| **W7B-E1** | CAP-418 Actor "anonymous, member" → staff-role set (E2's set); Gated-by "none" → **CAP-390 (any staff-role shell entry)**. Internal runbooks no longer publicly readable; sanitization still enforced. | CAP-418; wiki contract (header, §1, OQ#1) |
| **W7B-E5** | Readiness became a real gate: CAP-394/395 gain `launchReadinessResults` read; **CAP-480 setter now invokes the CAP-510 gate synchronously — `signup.mode=open` rejected fail-closed while any GATE is false** (not a warning). `waitlist`/`closed` always settable (closing down never blocked). CAP-510's admission-time check (FATAL-M1A-02) remains the independent second belt. Config + readiness contracts updated in both directions. | CAP-394/395/480/510; config contract (States H, Actions, OQ#2); readiness contract (States C, §4, OQ#1) |
| **W7B-E3** | **CAP-564 added** — `roles.revoke`, Administrator/Founder UI revoke (inverse of CAP-413's grant; CAP-009 CLI stays as emergency/scripted path). Writes roleAssignments (status=revoked, revokedAt) + auditLog. Next-request enforcement via CAP-430 unchanged. Register 563→564. | **CAP-564 (new)**; roles contract (header, §1, States G, Actions, OQ#1) |
| **W7B-E4** | **Investigated — both rows kept, not merged.** CAP-024 (M1, `timezone.correct`, Has-UI=NO) = the M1 **backend-contract row** ("timezone write-once; Admin+audit correction" — M1-platform §8/§14); CAP-404 (M15, `support.timezone.fix`, Has-UI=YES) = the M15 **UI action**. Two layers of one correction; the CAP-535-precedent check found genuine canonical status for both. **Canonical mutation name = `support.timezone.fix`** (M15 sheet §2 + MASTER-DRAFT mutation list cite only it); rows cross-linked, neither deleted. | CAP-024/404 Notes; support contract (§1, States C, OQ#1) |
| **W7B-E6** | **Investigated — no new capability needed.** "Per-action authority narrower than route authz" IS the second layer of E2's two-layer model: shell entry (any staff, CAP-390) → route (CAP-392) → each action's own per-CAP Actor column (CAP-221 Administrator, CAP-557 Moderator). No case found where neither layer covers a required check; both exemplar rows annotated. | CAP-221/557 Notes; shell contract OQ#2 |

### Wave 7C escalations (M12 Economy + M13 Moderation — 2 surfaces; ALL SEVEN CLOSED — founder decisions fully applied 2026-08-26)

*(Numbered W7C-* per the escalation IDs in RECONCILIATION-7C.)*

| ID | Resolution / status (one line) | CAP-IDs / files touched |
|---|---|---|
| **W7C-E-drift (L25)** | Leaderboard/Podium activation threshold set to **25 uniformly**. Register: CAP-194 ("~20 contributors" → "25 contributors") + CAP-294 ("min ~20 eligible" → "min 25 eligible"). Verification grep then found five further live occurrences, all fixed: MASTER-DECISION-DRAFT (DEC-M9-PODIUM, R-PODIUM, R-RECOG), M9-feed-discovery (R-PODIUM), M12-signals (R-RECOG), `_index.md` (DEC-M9-PODIUM echo), and the closed Wave-6 feed contract (States G). Data-model line 55 `leaderboard≥25` confirmed the single source of truth. Post-fix grep: zero threshold-20 occurrences remain (the sole residual string-hit is this row, quoting the pre-fix value). | CAP-194/294; MASTER-DECISION-DRAFT ×3; M9-feed-discovery; M12-signals; `_index.md`; wave-6 feed contract; `_data-model.md` line 55 (unchanged, authoritative) |
| **W7C-E-mod-1** | CAP-138 Actor corrected "Editor, Publisher, administrator" → **"Moderator, administrator"** — matching the `/admin/moderation` inventory (which already read correctly; the register row lagged). Editor/Publisher removed; no other surface affected (CAP-138's sole surface is this console). | CAP-138; moderation contract (header, §1, Actions, OQ#1) |
| **W7C-E-econ-2** | Public "Signals" bound to **`activeSignals`** — the live/current measure. Total = permanent historical record (not shown publicly as "Signals"); Pending transitional, not shown. CAP-281 Notes amended; profile-economy contract specifies it as a real value (States E, Entities, OQ#2 closed). | CAP-281; profile-economy contract |
| **W7C-E-econ-3** | `leaderboardOptOut` = **full hide** — Level/progress/badges AND Reach/Signals (the complete public economy-metrics surface: triad + ladder + badges), not a partial hide. Math unchanged. | CAP-312; profile-economy contract (States I, §1, OQ#3 closed) |
| **W7C wording (CAP-313)** | "two-component progress" was a miscount — corrected to **three-component progress (Reach% · Signal% · sustained-days)** on the register row and profile-economy contract (States G, OQ#6 closed). | CAP-313; profile-economy contract |
| **W7C-E-econ-1** | **CLOSED (founder decision, 2026-08-26):** CAP-313 Actor broadened `member` → **`anonymous, member`** — same ladder render for both. The silhouette-above-current-level mechanic is the gamification boundary, not a privacy boundary; no separate anonymous-restricted render exists. CAP-312's opt-out is the sole visibility governor (hides the full economy surface including the ladder); if not opted out, anonymous and member see identical ladder content. | CAP-313; profile-economy contract (§1, States G, Actions, OQ#1 closed) |
| **W7C-E-econ-6** | **CLOSED (founder decision, 2026-08-26):** **CAP-565 (M12) added** — System auto-creates a member's Distribution immediately following successful bootstrap completion (Actor: System; Reads: users bootstrapState=complete trigger; Writes: distributions ownershipMode=single, initial state; Has-UI: NO; Gated by: CAP-003 — fires immediately after, same session, NOT the same atomic transaction — deliberately not amending CAP-002/003's Writes). Guarantees CAP-299's 1:1 Distribution-per-member invariant before any member reaches /u/[handle]. Chose adjacent post-bootstrap action over amending CAP-002/003 (six waves of downstream dependencies on that transaction's exact shape); CAP-299 becomes effectively redundant at signup but stays as defensive/idempotent path (retry, migration, edge cases). Metrics tab default render: always a real (if empty/zero-state) Distribution, never null/missing-entity. Register 564→**565**. | **CAP-565 (new)**; CAP-313 (actor, E-econ-1); profile-economy contract (header, Route & Access/§1, States A, Actions, Entities, OQ#4 closed) |
| **W7C-E-mod-2** | **CLOSED (founder decision, 2026-08-26):** CAP-101/103/114 Writes amended to include `moderationCases` (polymorphic target: postShowcases row / mechanic-table row / toolRatings row) — the register's existing polymorphic case pattern (CAP-127/135/154/268/324; target typing matches CAP-533). Not a synthetic case — it IS the case; domain table = detail record, moderationCases = queue-visibility layer. All three surface in the **normal case-ordered queue (CAP-330), no special UI**. | CAP-101/103/114 Writes + Notes; moderation contract (header, Entities, States S, Actions ×3, OQ#2) |

### Wave 7D escalations (M16 Analytics + M18 Reliability + M17 UTM/SEO — 4 surfaces; ALL FOUR CLOSED — founder decisions applied 2026-08-26)

*(Numbered W7D-* per the escalation IDs in RECONCILIATION-7D. E2 = E-route.)*

| ID | Resolution (one line) | CAP-IDs / files touched |
|---|---|---|
| **W7D-E-route (E2)** | Route-name drift resolved — inventory canonical: CAP-479 trigger `/admin/utm-builder` → **`/admin/utm`**. DEC-M17-UTM rows (`_index.md` + MASTER-DECISION-DRAFT) + M17-sheet route references synced (5 spots); raw-input/legacy copies deliberately untouched as provenance. Residual: CAP-479 Actor=Founder/Admin vs inventory "administrator" scope drift stays open as unspecified detail (contract OQ#3). | CAP-479; `_index.md`; MASTER-DECISION-DRAFT ×4; M17-growth-seo ×4; UTM contract |
| **W7D-E1** | **CAP-566 (M17) added** — Admin seeds/edits UTM campaign source dictionary values (Actor: administrator; Writes: utmDictionary allowedSources/allowedMediums/campaign+content formats; Has-UI: YES; Gated by: shell-entry via CAP-390; mutation `utm.dictionary.seedEdit`). The CAP-536-class missing-writer hole — CAP-464/465/479 all read the dictionary, nothing seeded it; builder launched empty. Versioned per DEC-M17-UTM; feeds CAP-479 dropdowns + CAP-465 landing-page runtime validation. Register 565→566. | **CAP-566 (new)**; UTM contract (§1, Entities, States B, Actions, OQ#2 closed) |
| **W7D-E3** | **`thinIndexedCount` + `heldIndexedCount` added to `seoHealth`** in `_data-model.md` — backing fields for CAP-484's already-specified monitored predicates ("thin indexed = 0 · held indexed = 0"). CAP-484 Notes amended to confirm the fields now exist; no predicate change. | `_data-model.md`; CAP-484 Notes; SEO contract (Entities, States B/D, OQ#1 closed) |
| **W7D-E4** | **CAP-567 (M17) added** — Admin views SEO health dashboard (Actor: administrator; Reads: seoHealth; Writes: none; Has-UI: YES; Gated by: shell-entry via CAP-390; mutation `seo.health.view`). Render-only; cannot override `assertIndexable` (CAP-466 server-authoritative). Register 566→**567**. **Generalization check (verified from source, reported unfixed):** `/notifications` — gap REAL (CAP-386's "Member reads notifications" trigger is actually the mark-read mutation — its Writes column is `notifications.readAt`; the list render has no governing read CAP); `/admin/support` — gap NOT REAL (CAP-406 `support.userSummary` is a genuine read row: Reads listed, Writes: none). | **CAP-567 (new)**; SEO contract (§1, Actions, OQ#2 closed); reported finding in RECONCILIATION-7D |
| **W7-final (notifications list-read)** | **CLOSED (founder decision, 2026-08-26): CAP-568 (M14) added** — Member views notification list (Actor: member; Reads: notifications; Writes: none; Has-UI: YES; Gated by: none; mutation `notifications.list`). **Pre-add verification (CAP-535/CAP-544 discipline):** all four screen CAPs re-checked from source — CAP-378/379 = System quota emit-writers (write notifications+users marker), CAP-382 = System dedupe/batch writer, CAP-386 = member mark-read mutation (`notifications.readAt`) — **none is a list-render read; gap genuinely absent.** Same precedent as CAP-091 `posts.listByType`. Recipient-private; newest-first; pagination spec stays OQ#6. Closes 7A OQ#1 (originated there) + the 7D generalization finding. Register 567→**568**. Contract-phase status: **zero known gaps.** | **CAP-568 (new)**; notifications contract (header, §1, Entities, States F, Actions, OQ#1 closed); RECONCILIATION-7A (§1 row + non-escalated log) |

### Stage-2 audit corrections (2026-08-26 — AUDIT-FINAL.md applied)

| ID | Resolution | Evidence trail |
|---|---|---|
| **E3 + X1** | **Bootstrap-state routing RESOLVED (AUDIT-FINAL F-15 — the audit's one BLOCKING finding: "Bootstrap-state routing owned by no CAP: pending_context↔complete redirects 'will resurface on every protected screen' — flagged Wave 1, unresolved through Wave 7").** Closed by the **Platform-Wide Routing Convention** written into `CONTRACT-1-app-shell-FINAL.md` §1: (1) anonymous on protected route → `/signin`; (2) `pending_context` on any route ≠ `/welcome` → `/welcome`; (3) `complete` on `/signin`/`/waitlist`/`/welcome` → `/feed`; (4) server-side CAP-005 guard remains the security boundary — client routing is UX convenience. Not a new capability: makes explicit what CAP-001/002/003/005/030 already imply. Cross-referenced (not duplicated) in signin, welcome, waitlist, compose, setup contracts. | CONTRACT-1-app-shell §1; signin l.21+OQ2; welcome §1+OQ1–3; waitlist §1; compose §1+OQ7; setup §1 scope-note; AUDIT-FINAL Part C #3 |
| **E2 (F-05 sync)** | **CMP slot interim behavior RESOLVED — ledger catch-up (audit F-05 sub-item: "E2 resolved by cmp contract … yet OPEN in ledger").** `CONTRACT-7-cmp-FINAL.md` §1 declares: "This build resolves Wave-1 OPEN-DECISIONS E2 ('CMP slot interim behavior undefined')" — resolved 2026-08-25 when the CMP contract closed, but never moved to the archive. **Resolved behavior:** Phase 2's SLICE-P2-06 implements it as the reserved empty slot running CAP-504's default-deny posture (no analytics request without consent; no consent UI exists yet) — behaviorally identical to CAP-028's CMP-absent degrade, and the exact posture FATAL-M18-02 requires pre-grant. No consent machinery is invented pre-Phase-7. | CONTRACT-7-cmp §1 declaration; SLICE-CATALOG-PHASE2.md (SLICE-P2-06 acceptance criteria); AUDIT-FINAL F-05 |

### Phase-1 bible correction (2026-08-29 — described vs applied)

| ID | Resolution | Evidence trail |
|---|---|---|
| **X2 (F-13)** | **`waitlistEntries` field list written** — email · emailNormalized (unique) · status {waiting\|invited\|converted\|withdrawn\|blocked} · invitedAt? · convertedUserId? · createdAt. Derived from CAP-014/015/030 + M1 §7. Rate hashes stay in `@convex-dev/rate-limiter` (not on the row). Duplicate-entry UX remains waitlist-contract OQ#2. | `_data-model.md` waitlistEntries bullet + Core-enums `waitlistEntries.status`; SLICE-CATALOG-PHASE1 P1-02; AUDIT-FINAL F-13 |
