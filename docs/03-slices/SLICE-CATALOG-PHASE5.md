# SLICE CATALOG — BUILD PHASE 5: PEOPLE

**Date:** 2026-08-29 · **Phase order source:** AUDIT-FINAL.md Part D (corrected build order)
**Basis:** `CAPABILITY-REGISTER-MERGED.md` (570 rows) · `_data-model.md` · Wave 5A/5B contracts (personas, admin-personas, personas-genome, personas-queue, discussion-thread, setup, settings-profile, u-handle) · AUDIT-FINAL Part D
**Sizing-rule addendum applied:** every cited bullet checked for ellipsis/`{…}`/"see X" incompleteness. **Zero true ellipsis cases.** All M6/M7/M8 entity bullets verified complete inline (personas l.172, personaGenomes l.177, personaGenomeEdits l.186, personaCommentDrafts l.165, personaCommentEvaluations l.183, personaCadenceState l.182, personaRevivalVotes l.185, personaLifecycleEvents l.184, commentScores l.103, threadStats l.107, commentReactions l.81, activityLedger l.231, profiles/userProfileAttributes/userInterests/userInferences/userSocialAccounts/userConsentRecords/interestTaxonomy/profileCompletionEvents/postingEligibilityEvents l.58-67). Two bounded qualification notes, both resolvable in-slice with the stop-and-report guard: `systemConfig` keys for genome trait-ranges (genome contract OQ2) and waning triggers (admin-personas OQ2) are named nowhere — config-driven values, keys to be registered via Phase 3's typed-config pattern, not invented.
**Phase boundary:** personas (population/lifecycle/queue/genome), discussion engine, M7 posting gate + profile surfaces. Discussion **enriches** `/p/[slug]` owned by **P4-13** (SF-02) — P5-01/02/03 must not invent a second post-detail route. `/u/[handle]` Metrics enrichment (Reach/Signals/Awards/Ladder, CAP-281/297/299-305/313) is **Phase 7** — only Overview + Journal base tabs here. Cross-phase reads noted, not built.

---

## SLICE-P5-01 — M6 discussion spine schema (comments + reactions/saves/signals + scores/stats + read states)
- **CAP-IDs covered:** substrate for CAP-120–139 — schema only
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-discussion-thread-FINAL.md` (§2 Entities, §3 A/D/E/F/G/H/I)
- **Depends on:** SLICE-P4-01 (posts FK), SLICE-P4-13 (`/p/[slug]` base screen this thread enriches), SLICE-P1-03 (moderationCases — CAP-127/128 targets)
- **Scope:** Define `comments` (depth {0|1}, threadRootCommentId self-id convention, authorIntent?, deletedAt tombstone), `commentReactions` (valuable/negative mutual exclusivity server-enforced), `commentSaves`, `commentContextSignals`, `commentScores` (Bayesian bestScore projection, rankVersion), `threadStats`, `threadReadStates`, `userReadingProgress`, `threadPluginConfig` (typed keys only). Bible bullets l.81–115 complete; indexes for the sort modes (best/live/new/top/most_discussed/qa) + cursor-freeze support.
- **Files touched (expected):** `convex/schema.ts` (M6 region)
- **Acceptance criteria:** INV-1 (quoted): "One-reply-depth enforced"; INV-2 no-user-URLs carried from P4-02's R-URL. `valuable`/`negative` mutual exclusivity per (userId, commentId) server-enforced (quoted: "mutually exclusive, server-enforced"). `disagree` in a Debate thread "permanently barred from any quality-derived term" (quoted). Tombstone semantics (quoted): "soft-delete … replies preserved"; CAP-106's same-tx `postHelps.acceptedCommentId` clear representable.
- **Size check:** ≤2 days — ~9 tables, all transcription from complete bullets.

## SLICE-P5-02 — comments.create + eligibility gates + moderation pass (CAP-120/121/122/141/140-post + CAP-152/153/154)

> **UNBLOCKED 2026-09-04 — DECISIONS-LOCKED #1:** OTP provider = Twilio Verify (send/verify/expire/retry via Twilio API; no custom OTP logic). CAP-551 buildable; CAP-141 comment eligibility unblocked.
- **CAP-IDs covered:** CAP-120, CAP-121, CAP-122, CAP-141, CAP-140, CAP-152, CAP-153, CAP-154 · CAP-155 *(added 2026-09-04 — orphan disposition: low-trust outbound-link gate on comment links; the same helper serves compose-side links in P4-02 — one implementation, two call sites)*
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-discussion-thread-FINAL.md` (§4 rows 1–3) · `contracts/wave-5/CONTRACT-5-setup-FINAL.md` (§1 gate logic)
- **Depends on:** SLICE-P5-01 (schema), SLICE-P4-13 (`/p/[slug]` host), SLICE-P4-15 (CAP-098 Help row exists for CAP-122 same-tx clear), SLICE-P2-03 (assertCustomerCapability — `create_comment`/`create_post` keys), SLICE-P1-03 (moderationCases + auto-gate stub), SLICE-P4-02 (R-URL reuse)
- **Scope:** Implement `comments.create` with the full gate chain (M7 R-ELIGIBILITY comment path: email+mobile verified + active + not-restricted, NO profile gate — CAP-141 verbatim; M13 auto-mod pass; INV-1 depth; INV-2 no-user-URLs), `comments.edit` (ownership), `comments.softDelete` (tombstone + postHelps clear same-tx). Same-mutation writes: parent replyCount patch, threadStats deltas, commentScores dirty-flag, atomic rawEvents append. Wire CAP-140's post-path eligibility machine (postingEligibilityState transitions + append-only events, draft-preserve on basic_incomplete) and CAP-152/153/154's rate/deterministic/safety gates at the composer boundary — these are the gates P4-02 stubbed. Routing: composer→/setup redirect is F-34's unowned client behavior — implemented here as the natural home, flagged, non-blocking either way.
- **Files touched (expected):** `convex/comments.ts`; `convex/eligibility.ts`; `convex/activity.ts` call-site (CAP-570 comment_created); rawEvents catalog rows (CAP-437-gated, names flagged — none named in register)
- **Acceptance criteria:** CAP-141 (quoted): "Comment = email+mobile verified; no profile gate." CAP-140 (quoted): "On incomplete → preserve draft, return missing basic decisions. State machine persisted append-only." CAP-120 (quoted): "Same-mutation parent replyCount + threadStats + dirty-flag. atomic rawEvents append." CAP-122 (quoted): "clears postHelps.acceptedCommentId same transaction. Replies preserved." CAP-152 (quoted): "N posts/hour, tier-independent. O(1) rolling counter, compute-at-write. Not a distribution gate." CAP-153's deterministic list and CAP-154's "Fail-closed" quoted. F-34 noted as OPEN in OPEN-DECISIONS — client redirect ships best-effort, server enforcement already fail-safe.
- **Size check:** ≤2 days, full — the eligibility machine + three abuse gates + comment CRUD is the biggest member-facing slice. Split line if needed: comment CRUD+gates (this slice) from post-path abuse gates (CAP-152/153/154 could move to a P5-02b riding P4-02's composer) — flagged, not pre-split.

## SLICE-P5-03 — Comment reads + reactions + saves + signals + read-state (CAP-123/124/125/126/127/128/131/139)
- **CAP-IDs covered:** CAP-123, CAP-124, CAP-125, CAP-126, CAP-127, CAP-128, CAP-131, CAP-139
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-discussion-thread-FINAL.md` (§4 rows 4–11)
- **Depends on:** SLICE-P5-01 (schema), SLICE-P5-02 (comments exist + gate helpers), SLICE-P4-13 (thread mounts on post detail, not a standalone route), SLICE-P2-03 (customer guard for react), SLICE-P5-05 (activity.append helper for upvote_given/save_added call-sites)
- **Scope:** Implement the six sort modes with cursor-rankVersion freezing, the thread page query with MAX refs (rendering empty until Phase 7's CAP-132 — layering caveat stated), valuable/negative reactions (mutual exclusivity, negative hidden with private reason), saves, context signals (hidden-until-threshold), read-state + reading-progress writes, realtime hot-window push (≤100/≤5/≤50 bounds, snapshot-stable). Anonymous-safe vs member branches on every public read.
- **Files touched (expected):** `convex/comments/reads.ts`; `convex/reactions.ts` (CAP-570 upvote_given/save_added same-mutation call-sites); realtime subscription layer
- **Acceptance criteria:** Six sort modes (quoted): "best (Bayesian confidence-damped … NOT Wilson; one numerator) · live · new · top · most_discussed · qa". CAP-123 (quoted): "Cursor-based; freezes rankVersion per session." CAP-125 (quoted): "The single positive numerator for Best. Weight = signalReputation + legitimacy, never Recognition." CAP-128 (quoted): "Hidden result; no public count; never lowers Best (INV-3)." CAP-139 hot-window bounds (quoted): "≤100 live top-level, ≤5 expanded groups, ≤50 replies/group, 30-min sub refresh. Snapshot-stable." Negative `reason` PRIVATE — read-scoping enforced server-side.
- **Size check:** ≤2 days — read-heavy but mechanical; the six sort queries share one index scaffold. MAX panel renders placeholder-only (compute is Phase 7).

## SLICE-P5-04 — M6 rank recompute + decay + inference tick (CAP-129/130/145)

> **UNBLOCKED 2026-09-04 — DECISIONS-LOCKED #11:** rank/score constants ship as versioned config defaults tagged `calibration_pending: true` (Hot = time-decay gravity, Top = recency+engagement blend); no hardcoded values; real calibration post-beta gated by Readiness Category 8.
- **CAP-IDs covered:** CAP-129, CAP-130, CAP-145
- **Source contract(s):** register rows CAP-129/130/145 · discussion contract §2 (commentScores dirty-flag contract)
- **Depends on:** SLICE-P5-01 (commentScores schema), SLICE-P1-04 (jobCatalog registration)
- **Scope:** Implement the ~3s dirty-flag recompute cron (leased bounded batches, clears flag before read, idempotent vs overlap), the scheduled decay tick (liveScore only), and CAP-145's inference batch (≥3 qualifying events across ≥2 sessions; prohibited-inference list enforced; versioned userInferences with manifest provenance). Register as jobCatalog jobs.
- **Files touched (expected):** `convex/jobs/rank.ts`; `convex/jobs/decay.ts`; `convex/jobs/infer.ts`
- **Acceptance criteria:** CAP-129 (quoted): "Leased bounded batches; clears flag before read; idempotent vs overlap." CAP-130 (quoted): "Live sort only." CAP-145 (quoted): "Never blocks. ≥3 qualifying events across ≥2 sessions. Prohibited inferences: age/gender/income/etc. Lightweight manifest provenance (not per-event pointers)."
- **Size check:** ≤2 days comfortably — three small crons over Phase 1's job framework.

## SLICE-P5-05 — M7 profile schema + /setup + interests + activityLedger writer (CAP-142/144/148/570 + interestTaxonomy seed)
- **CAP-IDs covered:** CAP-142, CAP-144, CAP-148, CAP-570, CAP-534-adjacent (taxonomy read only; interestTaxonomy SEED added here per the CAP-536 pattern — contract OQ4 verified unresolved)
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-setup-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P1-01a/b (users/privateUserData), SLICE-P5-02 (eligibility machine), SLICE-P1-08 (deploy-seed pattern)
- **Scope:** Define `profiles`/`userInterests`/`userConsentRecords`/`profileCompletionEvents`/`postingEligibilityEvents`/`interestTaxonomy`/`userProfileAttributes`/`userSocialAccounts`/`userInferences`/`activityLedger` schema (complete bullets l.58-67, l.231). **CAP-570 `activity.append` helper** — the same-mutation ledger writer resolving ASYMMETRY #1 (CAP-570 added to the register 2026-08-29 per founder approval): one shared append helper `activity.append(userId, eventType, targetType, targetId, summary, metaTagged)` fired inline from the six v1 event types' owning writers — **post_published** (CAP-055/086 — Phase 4 wiring point, P4-04/05), **comment_created** (CAP-120 in P5-02; CAP-173 persona writer in P5-11), **upvote_given** (CAP-125 in P5-03), **save_added** (CAP-126 in P5-03), **resource_acquired** (Phase 6 M10 writer), **tier_unlocked** (Phase 7 M12 writer). The helper + activityLedger schema land HERE; member-facing call-sites wire in their owning slices (P5-02/03/11), and Phase 6/7 wiring points are named in those phases' catalogs. Mirrors CAP-436's rawEvents same-mutation discipline. Build `/setup`: six-item required set (verified precondition, auto-filled display name, default avatar, ≥1 interest tap, rules accept, age/COPPA), `profile.upsertBasic` writing all six + the CAP-142 users fields, interest tiles from `interestTaxonomy`, `profile.consentRecord` per-field defaults (interests ON, demographics OFF, behavioral ON, public ON), `interests.select/remove`. **interestTaxonomy deploy seeder** — RECONCILIATION-5B verified no CAP seeds it (CAP-022 categories/config only; CAP-534 is post-tag taxonomy; CAP-389 disables). Seed derives tiles from the post-type/M5 registry per the bible's derivation note; keys land in configKeyRegistry. CAP-551 (mobile OTP) fenced: FOUNDER-DECISION-M7-01 (SMS provider) is OPEN — schema fields exist, UI fenced, mutation not buildable until locked.
- **Files touched (expected):** `convex/schema.ts` (M7 region); `convex/activity.ts` (CAP-570 helper); `convex/setup.ts`; seeder; `app/setup/` route
- **Acceptance criteria:** CAP-570 (quoted, register Notes): "System appends an activityLedger row in the same mutation whenever one of the six v1 event types fires" — helper signature covers userId, eventType (six v1 names verbatim: post_published · comment_created · upvote_given · save_added · resource_acquired · tier_unlocked), targetType, targetId, summary, meta with **"each meta field MUST be tagged safe-for-public vs always-private"** (bible l.231, quoted), visibility defaults `{private|public}` = private. CAP-142 (quoted): "Unlocks posting. Verified + display name (auto-filled) + avatar (default) + ≥1 interest tap + accept rules + age/COPPA confirm" + Writes including "users (rulesAcceptedVersion, legalAgeAssertedVersion)". CAP-148 defaults (quoted): "(interests ON, demographics OFF, behavioral ON, public ON)". CAP-144 (quoted): "Direct source stored separately from inferred." Skip/abandon (quoted): "partial completion persists; posting stays basic_incomplete". CAP-551 note (quoted): "FOUNDER-DECISION-M7-01 required before this capability is buildable" — fenced, not built.
- **Size check:** ≤2 days — schema + one screen + one composite mutation + one seeder + one append helper (single shared function; the six writer call-sites wire in their owning slices, so no scope growth here). The CAP-551 fence keeps OTP vendor work out.

## SLICE-P5-06 — /settings/profile writes + erasure + badges (CAP-143/146/147/149/150/151/157/549/552)
- **CAP-IDs covered:** CAP-143, CAP-146, CAP-147, CAP-149, CAP-150, CAP-151, CAP-157, CAP-549, CAP-552
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-settings-profile-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-05 (schema + consent flags), SLICE-P5-04 (userInferences exists for CAP-151 invalidation), SLICE-P1-06 (audit writer)
- **Scope:** Implement the write surface: `profile.setAttribute` (consent-bound, prefer-not-to-say = completed decision), `social.add` / stub `social.verify` / `social.revoke` (soft-delete per CAP-545 precedent), `profile.consentWithdraw` (triggers CAP-151 cascade), `erasure.detachAttribute` (inferences invalidated + attributes detached + non-value-bearing audit record — erased values NEVER in auditLog.prev), `completion.recompute` (per-field badges, Recognition-firewalled), the CAP-157 re-acceptance global overlay (`consent.reaccept`), and CAP-552 privacy toggles. Age-band income/spend (Phase-2 fields) not offered.
- **Files touched (expected):** `convex/profile/settings.ts`; re-acceptance overlay component
- **Acceptance criteria:** CAP-151 (quoted): "Invalidates derivation trail source records. Recognition persists as 'completed under version X' without revealing answers. Aggregates survive only if unlinkable. Never retain erased values in auditLog.prev." CAP-549 (quoted): "Soft-delete, matching CAP-545's precedent — preserves audit trail." CAP-150 (quoted): "Per-field badges, not gamified 100% bar … Firewall: Recognition only, never Signal/rank/reach." CAP-148/149 mutation names post-Wave-7A-E2 (quoted): "`profile.consentRecord` / `profile.consentWithdraw` — per-field PROFILE consent, scoped distinctly from M18's platform-wide CMP". CAP-552 (quoted): "Both are simple boolean/enum settings toggles … member self-preference on `users`, not PII-class like mobile."
- **Size check:** ≤2 days — nine capabilities but seven are one-pattern mutations; erasure cascade is the precision item.

## SLICE-P5-07 — /u/[handle] base tabs + handle reserve (CAP-526/527/528/550)
- **CAP-IDs covered:** CAP-526, CAP-527, CAP-528, CAP-550
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-u-handle-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-05 (profiles/schema), SLICE-P4-05 (badges schema), SLICE-P2-01/02 (bootstrap — CAP-550 fires at profile creation)
- **Scope:** Implement `handle.reserve` (System, at profile creation, username/usernameNormalized uniqueness — CAP-474 discipline; CHANGE is FUTURE-M7-01, not built), the Overview tab (identity + per-field badges + Awards/M12 shelf as reserved empty placeholders — honest zero-state), Journal Summary + Ledger (self-only at launch; activityLedger reads, paginated). Metrics tab structure reserved, content Phase 7. noindex per CAP-486.
- **Files touched (expected):** `convex/profile/page.ts`; `app/u/[handle]/` route
- **Acceptance criteria:** CAP-526 (quoted): "Overview tab: identity, per-field completion badges (CAP-150), Awards shelf (CAP-297) … Single query surface." CAP-527 (quoted): "ASSUMPTION: self-only visibility at launch" — enforced: Journal hidden for non-self. CAP-550 (quoted): "Mirrors CAP-474's slug governance … Handle CHANGE after initial assignment is not built — FUTURE-M7-01." Placeholder honesty (quoted from contract §3 A): "render empty/placeholder until W7".
- **Size check:** ≤2 days — three read queries + one System reserve mutation + tabbed page.

## SLICE-P5-08 — M8 persona spine schema + generation/evaluation chain (CAP-158/168/169/170/171 + genome tables)
- **CAP-IDs covered:** CAP-158, CAP-168, CAP-169, CAP-170, CAP-171 (substrate schema for all of M8)
- **Source contract(s):** register rows CAP-158/168-171 · `_data-model.md` l.165-186
- **Depends on:** SLICE-P4-01 (posts), SLICE-P5-01 (comments), SLICE-P1-04 (jobCatalog), SLICE-P1-05 (config keys for thresholds)
- **Scope:** Define all persona tables (personas with SEALED systemPrompt, personaGenomes, personaGenomeEdits, personaPositions, personaMemoryEmbeddings with by_persona_embedding vector index, personaEngagements, personaStyleBaseline, personaCadenceState, personaCommentDrafts, personaCommentEvaluations, personaLifecycleEvents, personaRevivalVotes). Implement the generation chain as System actions: `cadence.recompute` (density ≤15%, selectivity gate), `selection.relevanceGate` (default-to-silence), `persona.generateComment` (GLM, memory personaId-scoped, thread text untrusted), `persona.evaluate` (hard-kill BEFORE operator + soft 0-5 advisory), `genome.compileSystemPrompt` (CAP-158 — the compiled-prompt cache CAP-547 invalidates).
- **Files touched (expected):** `convex/schema.ts` (M8 region); `convex/persona/generate.ts`
- **Acceptance criteria:** CAP-168 (quoted): "Selectivity is the lever: most posts = 0. Density ≤15% of human comments / 24h. Real timestamps." CAP-169 (quoted): "Default-to-silence (INV-1). Do-not-generate = SUCCESS. Score authorizes generation, NEVER publication." CAP-170 (quoted): "Memory retrieval personaId-scoped (INV-4). Thread text = untrusted (prompt-injection defense). No GLM mutation/publish access." CAP-171 (quoted): "Hard auto-kill BEFORE operator (INV-5). Soft 0-5 operator-facing, never gate." CAP-158 (quoted): "systemPrompt is COMPILED from genome, not hand-written" — sealed, never public (CAP-180 E-H allowlist enforced at query projection).
- **Size check:** ≤2 days, full — schema (13 tables) + generation chain. The public/queue/lifecycle surfaces are deliberately P5-09/10 to keep this at the engine. Split line if needed: schema+compile (P5-08a) from generate/evaluate chain (P5-08b) — flagged.

## SLICE-P5-09 — Persona public surfaces: /personas + persona profile (CAP-179/180/181/176/177)
- **CAP-IDs covered:** CAP-179, CAP-180, CAP-181, CAP-176, CAP-177
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-personas-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-08 (personas schema + chain), SLICE-P2-03 (customer guard — revival_vote key), SLICE-P5-02 (eligibility reads for voter gating)
- **Scope:** Build `/personas` (roster by lifecycle section: active/newly-arrived/waning/retired + server-computed human-vs-AI counter) and the persona profile page (`/personas/[id]` — identity + AI label + track record + "how this AI thinks" from identityCharter; genome public-safe allowlist per E-H). Revival voting (CAP-176, trust-tier + account-age gated, unique per user+persona, rate-limited; CAP-393 guard), tally view (CAP-177), bring-back UI entry (CAP-181). Anonymous-safe branch throughout.
- **Files touched (expected):** `app/personas/` routes; `convex/persona/public.ts`
- **Acceptance criteria:** CAP-180 E-H (quoted): "public query projection is bound to the genome **public-safe allowlist** … Sealed genome fields (exact prompt template, generation weights/parameters, internal rules, compiled `systemPrompt`) MUST NEVER appear in any public-facing query response." CAP-179 (quoted): "Computed server-side from authorType." CAP-176 (quoted): "One vote per member. Brigading → suspicious spikes flagged." Revival never auto-fires — CAP-165 operator confirm (P5-10).
- **Size check:** ≤2 days — two public pages + one gated mutation + tally.

## SLICE-P5-10 — Persona lifecycle console + crons (CAP-159-167)
- **CAP-IDs covered:** CAP-159, CAP-160, CAP-161, CAP-162, CAP-163, CAP-164, CAP-165, CAP-166, CAP-167
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-admin-personas-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-08 (schema), SLICE-P5-09 (revival votes exist), SLICE-P3-01/04/05 (shell + A1 + A12), SLICE-P1-06 (audit)
- **Scope:** Build `/admin/personas`: the full lifecycle console — birth (recommended-confirm or hand-craft, QA gate: genome-diversity embed + 5 test comments + name-collision + operator approval, INV-7 max 1 birth/day), activate (trial ≤3 comments + ≥N days — N config-keyed, unspecified value flagged), pause/resume (Moderator admitted on pause), wane (System auto-fire + operator; 4 trigger branches), retire (triggers or operator, INV-7 max 1/day, graceful, profile+history preserved), revive-confirm (threshold + re-QA + tally snapshot). The two crons: population.recommend (daily) + drift.check (weekly, flag-only). All operator mutations write auditLog; crons write none (queue outputs only).
- **Files touched (expected):** `convex/persona/lifecycle.ts`; `convex/jobs/population.ts`; `convex/jobs/drift.ts`; `/admin/personas` page
- **Acceptance criteria:** CAP-159 (quoted): "Never born because others retired. INV-7: max 1 birth/day." CAP-163/164 soft-lifespan (quoted): "Soft lifespan 3–6mo, operator-extendable" / "Graceful; profile + history preserved. INV-7: max 1 retirement/day." CAP-165 (quoted): "Snapshots eligible tally + threshold at confirmation. Restores same persona (full memory + lifecycle history). Never auto." CAP-166 (quoted): "Recommends births/retirements; admin executes." CAP-167 (quoted): "flag for operator (re-ground or retire)" — no auto status change. E-E per-action actors respected (pause admits Moderator; wane includes System).
- **Size check:** ≤2 days, borderline — 7 lifecycle mutations + 2 crons + console, but each mutation is the same pattern (write personas + lifecycleEvent + auditLog under a gate). The 3 unspecified config values (trial-N, waning thresholds, regen cap) are config-keyed with flagged defaults, not invented semantics.
- **FATAL-adjacent flag:** the generation-vs-publication authority boundary — CAP-169's "Score authorizes generation, NEVER publication" and CAP-171's hard-kill-before-operator — is quoted in P5-08 and re-verified here at the console boundary (a waned/retired persona must never feed the queue). Any operator action that could bypass CAP-173 approval is a blocker.

## SLICE-P5-11 — Persona comment review queue (CAP-172/173/174/175)
- **CAP-IDs covered:** CAP-172, CAP-173, CAP-174, CAP-175
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-personas-queue-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-08 (drafts + evaluations), SLICE-P5-02 (comments.create rules — CAP-173 publishes via M6), SLICE-P5-05 (activity.append helper for comment_created call-site), SLICE-P3-05 (A12 board), SLICE-P3-06 (datetime picker for scheduledFor)
- **Scope:** Build `/admin/personas/queue`: regen (CAP-172, cap 2–3, supersede chain, chronic fail → waning handoff), approve/publish (CAP-173 — writes comments via M6 rules + engagements + positions + memory embeddings + staggered timestamp), reject (CAP-174, terminal-for-draft), schedule (CAP-175 — scheduledFor distinct from earliestPublishAt; scheduled fire re-validates persona not paused/retired, flagged as contract OQ3's register-silent edge — ship fail-closed: hold + queue alert). Manual draft-body edit (the CAP-543 analog) is a contract Open Question — schema supports `editedBody?`/status=edited but no CAP owns the write; NOT built, flagged.
- **Files touched (expected):** `convex/persona/queue.ts` (CAP-570 comment_created call-site on CAP-173 publish); `/admin/personas/queue` page
- **Acceptance criteria:** CAP-172 (quoted): "Capped" + "chronic fail → CAP-163 waning". CAP-173 (quoted): "Staggered real timestamp (INV-9). Persona-excluded from M6 rank/counts (INV-6)." CAP-175 scheduled enum + `scheduledFor` per E-B (quoted): "same status-enum pattern as contentCandidates.status=scheduled for CAP-054". Scheduled-fire fail-closed on paused/retired persona (derived from CAP-161/164 states — stated, not register-quoted; flagged).
- **Size check:** ≤2 days — four operator mutations over the A12 board; publish fan-out reuses P5-02's comment-create path.

## SLICE-P5-12 — Genome config back-door + rollback + preview + INVALIDATION same-slice (CAP-178/546/547/548)
- **CAP-IDs covered:** CAP-178, CAP-546, CAP-547, CAP-548
- **Source contract(s):** `contracts/wave-5/CONTRACT-5-personas-genome-FINAL.md` (§1–§4)
- **Depends on:** SLICE-P5-08 (genome tables + CAP-158 compile helper), SLICE-P3-01 (shell entry — Administrator-only), SLICE-P1-06 (audit fail-closed)
- **Scope:** Build `/admin/personas/genome` (back-door route, no nav item, Administrator-only): the three config modes (tune template / override instance / hand-craft), every edit versioned + preview-fixtured + personaGenomeEdits + auditLog, CAP-546 rollback (append-only history, rollback writes a NEW edit event), CAP-548 preview fixture (generate from draft/unsaved params). **CAP-547 invalidation is IN THIS SLICE, same transaction as every genome write and rollback** — discards the CAP-158 in-memory compiled prompt + persisted personas.systemPrompt; template-scope edits invalidate every instance compiled from that template. This is the hazard the phase prompt names: invalidation never splits from the write path.
- **Files touched (expected):** `convex/persona/genome.ts`; `/admin/personas/genome` page
- **Acceptance criteria:** CAP-547 (quoted): "Without this, live persona comment generation can silently use a stale compiled prompt after an edit or rollback — worse than the affiliate-cascade gap because it fails silent, not visible. Write target is the actual CAP-158 compiled-prompt cache … plus persisted `personas.systemPrompt`. Fires on CAP-178 edit and CAP-546 rollback." Genome contract §3 C (quoted): "Genome edit (CAP-178) or rollback (CAP-546) **must** fire **CAP-547** … A `scope=template` edit invalidates every instance compiled from that template." CAP-546 (quoted): "rollback writes a new personaGenomeEdits event (does not rewrite prior rows)." CAP-548 (quoted): "Writes the existing `personaGenomeEdits.previewFixtureRef` field (not a new entity)." Audit-fail → fail-closed (CAP-426 pattern). In-flight drafts insulated (quoted): "personaCommentDrafts.genomeVersion snapshots at generation."
- **Size check:** ≤2 days — one restricted console + versioned writes + rollback + preview; the invalidation is one helper called in the same transaction as write/rollback (deliberately NOT its own slice).
- **FATAL-adjacent flag:** silent-failure class (the phase's hazard #1). Acceptance requires an e2e test: edit genome → next generate recompiles (no stale prompt observable). Splitting invalidation out of this slice is prohibited.

---

## Dependency graph (within Phase 5)

1. **SLICE-P5-01** (M6 schema) — foundational for 02/03/04
2. **SLICE-P5-05** (M7 schema + /setup + seed) — parallel with P5-01 track; foundational for 06/07
3. **SLICE-P5-02** (comments + eligibility + abuse gates) — after 01 + 05(schema); wires P4-02's composer gate stubs
4. **SLICE-P5-03** (reads + reactions + realtime) — after 02
5. **SLICE-P5-04** (rank/decay/infer crons) — after 01; parallel with 02/03
6. **SLICE-P5-06** (settings + erasure + badges) — after 05
7. **SLICE-P5-07** (/u/[handle] + handle.reserve) — after 05 + 06 (badges render)
8. **SLICE-P5-08** (M8 schema + generation chain) — after 01 + P1-04; parallel with the M7 track
9. **SLICE-P5-09** (/personas + profile + revival) — after 08
10. **SLICE-P5-10** (lifecycle console + crons) — after 08 + 09; parallel with 11
11. **SLICE-P5-11** (comment queue) — after 08 + P3-05/06; parallel with 10
12. **SLICE-P5-12** (genome back-door + rollback + invalidation) — after 08; parallel with 10/11. **CAP-547 invalidation ships same-slice with CAP-178/546 writes — never split.**

**Phase exit gate (audit Part D, quoted):** "persona comment passes review queue; comment gate enforces verify+consent." Concretely: a persona draft survives hard-kill → operator approves → comment publishes AI-badged, rank-excluded; a member comment passes email+mobile-verify + consent checks with no profile gate; a genome edit invalidates the compiled prompt in the same transaction.

---

## M7 write/read symmetry pass (hazard #2 — findings, pre-build)

Checked every field this phase's slices WRITE against a read/display/teardown path in-phase:

| Write (slice) | Read/display | Teardown | Verdict |
|---|---|---|---|
| profiles/userProfileAttributes (P5-05/06) | CAP-526 Overview (P5-07) | CAP-151 erase/detach (P5-06) | Symmetric |
| userSocialAccounts add (P5-06) | CAP-526 + CAP-230 store-request gate (Phase 6 read) | CAP-549 revoke (P5-06) | Symmetric |
| userConsentRecords (P5-05/06) | read in same rows (CAP-142/149/157) | withdrawal append (P5-06) | Symmetric |
| userInferences (P5-04 cron) | CAP-526 bound display; inferred-vs-declared surface (settings §6 gap noted) | CAP-151 invalidation (P5-06) | Symmetric (display gap flagged contract-side: no inferred-vs-declared §11 component) |
| postingEligibilityEvents (P5-02) | CAP-140 state machine reads | append-only by design | Symmetric |
| activityLedger ( eventType v1 starter) — **writer check** | CAP-527/528 Journal (P5-07) | append-only by design | **RESOLVED (founder-approved 2026-08-29):** **CAP-570** `activity.append` added to the register (register 569→570) — same-mutation append from the six v1 event types' owning writers (post_published ← CAP-055/086; comment_created ← CAP-120/173; upvote_given ← CAP-125; save_added ← CAP-126; resource_acquired ← Phase 6 M10; tier_unlocked ← Phase 7 M12). Helper + schema implementation rides **P5-05** (owns the activityLedger schema bullet and lands before every member-facing writer slice); call-sites wire in P5-02/03/11; Phase 6/7 wiring points named in those phases' catalogs. Journal tabs no longer render empty. |
| threadReadStates/userReadingProgress (P5-03) | CAP-131 jump-to-unread (same slice) | — (projection) | Symmetric |
| personaCommentDrafts statuses (P5-11) | queue render (P5-11) | CAP-174 reject + supersede (P5-11) | Symmetric |
| personaEngagements/Positions/MemoryEmbeddings (P5-08/11) | CAP-180 "how this AI thinks" (P5-09) + revival restore (P5-10) | — (append-only ledgers) | Symmetric |
| personas.systemPrompt compiled (P5-08) | SEALED — no public read by design (CAP-180 allowlist) | CAP-547 invalidation (P5-12) | Symmetric (sealed read is intentional, E-H) |
| users.username/usernameNormalized (P5-07 CAP-550) | route resolution (P5-07) | FUTURE-M7-01 (tracked, immutable by design) | Symmetric (deferral is a founder decision, not a gap) |
| users.mobileVerified + privateUserData.mobileNumber (CAP-551) | CAP-141 comment gate (P5-02) | — | **ASYMMETRY #2 (vendor-fenced):** read path builds in P5-02; write path FENCED on FOUNDER-DECISION-M7-01 (SMS provider). Consistent — flagged fence, both sides stated. |
| profiles.completionVersion/badges (P5-06) | CAP-526 badge render (P5-07) | — | Symmetric |

**Two flagged asymmetries at slicing time, one since resolved, zero silently-built asymmetries.** #1 (activityLedger writer) was resolved by founder approval of **CAP-570** (2026-08-29): register row added, implementation rides P5-05, call-sites in P5-02/03/11, Phase 6/7 wiring points named forward. #2 is the known vendor fence restated where its read-side lands.

---

**Flags carried forward (stated, not silent):** FOUNDER-DECISION-M7-01 (CAP-551 OTP vendor); FUTURE-M7-01 (handle change); persona manual draft-edit writer (queue OQ1 — schema supports, no CAP); pre-activation discard path (admin-personas OQ4); queue list query unnamed (OQ6); waning-threshold/trial-N/regen-cap config keys unnamed (typed-config registration in-slice); CAP-366 engage-bit writer (M14, Phase 7); CAP-145 inference read-side display component gap (§11); public-read branch field lists unspecified (standing rule — anonymous-safe minimal sets derived in-slice, flagged). **CAP-160 "typo" retracted:** re-verified against contract §2 AND §4 on closure (2026-08-29) — the register's Reads (`personas, personaLifecycleEvents, personaCommentEvaluations`) matches the contract's grounding exactly (`personaCommentEvaluations` = "activation trial evidence" for CAP-160; `personaCadenceState` is **CAP-164-only**, retirement triggers). No register change; the flag originated from a misreading in this catalog's own symmetry pass and is withdrawn. **CAP-570 note:** post_published's writer (CAP-055/086) and resource_acquired/tier_unlocked's writers live in Phases 4/6/7 — P5-05 ships the helper + schema; those wiring points are restated in the owning phases' catalogs when sliced.
